# Config Directory

This directory contains configuration files for the PLC-GBT project.

## Files

- `config.py` - Main Python configuration file containing project settings

## Usage

Import configuration settings in your Python scripts:

```python
from config.config import SETTING_NAME
```

## Note

Configuration files may contain sensitive information. Ensure `.env` files and secrets are properly managed and not committed to version control.
