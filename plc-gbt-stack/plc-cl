#!/usr/bin/env python3
"""
PLC Control Loop CLI - Main Entry Point

This script properly sets up the Python path and launches the CLI.
"""

import sys
import os
from pathlib import Path

# Add the plc-gbt-stack directory to Python path
script_dir = Path(__file__).parent
sys.path.insert(0, str(script_dir))

# Import and run the CLI
from cli.plc_control_loop_cli import cli

if __name__ == "__main__":
    cli() 